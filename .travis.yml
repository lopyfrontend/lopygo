language:      node_js
node_js:
  - "8"
skip_cleanup: true
before_install:
  - lsb_release -a
install:
  - npm i -g yarn
  - yarn global add webpack webpack-cli
  - yarn install
script:
#  - npm test
  - yarn run dev
  - yarn run build
#after_script:
#  -
cache:
  directories:
    - node_modules
notifications:
  email:       false
deploy:
  provider: releases
  api_key: $GITHUB_OAUTH_TOKEN
  skip_cleanup: true
  on:
    tag: release